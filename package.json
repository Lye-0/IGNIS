{
  "name": "ignis-fire-study",
  "version": "2.0.0",
  "private": true,
  "description": "An offline, interactive volumetric fire study.",
  "scripts": {"start": "node scripts/serve.mjs", "test": "node --test tests/*.test.mjs", "build": "node scripts/build.mjs", "preview:build": "node scripts/build-preview.mjs"},
  "engines": {"node": ">=18"}
}
